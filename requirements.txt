# 基础数据处理
numpy==1.26.4
pandas==2.1.4
matplotlib==3.8.2
seaborn==0.13.1

# 计算机视觉与图像处理
opencv-python==4.8.1.78
Pillow==10.1.0
torchvision==0.22.0+cu124  # 与PyTorch 2.7.0配套的CUDA 12.4版本

# 深度学习框架
torch==2.7.0+cu124         # 核心：RTX 5090适配的CUDA 12.4版本（最新稳定）
tensorboard==2.15.1

# 数据加载与进度显示
tqdm==4.66.1
scikit-learn==1.3.2

# 可选：若驱动仅支持CUDA 12.1（RTX 5090最低兼容版本）
# torch==2.7.0+cu121
# torchvision==0.22.0+cu121

# 兼容项：PyTorch 2.7.0需适配的依赖补充
typing-extensions>=4.8.0